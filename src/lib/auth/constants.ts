/** Session with JWT + mapped portal user (includes client-only national_id for tickets). */
export const AUTH_STORAGE_KEY = 'svmp_portal_auth_v2';

/** Legacy demo-only storage — cleared on init when using API auth. */
export const LEGACY_USER_STORAGE_KEY = 'svmp_portal_user_v1';
